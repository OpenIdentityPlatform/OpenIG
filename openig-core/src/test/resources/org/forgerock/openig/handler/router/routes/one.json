{
    "heap": [
        {
            "name": "StatusHandler",
            "type": "org.forgerock.openig.handler.router.StatusHandler",
            "config": {
                "status": 418
            }
        }
    ],
    "handler": "StatusHandler",
    "condition": "${attributes.name == 'OpenIG'}"
}
