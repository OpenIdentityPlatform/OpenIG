{
    "heap": [
        {
            "name": "Detection",
            "type": "org.forgerock.openig.handler.router.DestroyDetectHandler",
            "config": { }
        }
    ],
    "handler": "Detection",
    "condition": "${exchange.attributes.test == 'Destroy'}"
}
