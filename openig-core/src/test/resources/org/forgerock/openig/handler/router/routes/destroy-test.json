{
    "heap": [
        {
            "name": "Detection",
            "type": "org.forgerock.openig.handler.router.DestroyDetectHandler",
            "config": { }
        }
    ],
    "handler": "Detection",
    "condition": "${contexts.attributes.attributes.test == 'Destroy'}"
}
