{
    "handler": {
        "type": "DispatchHandler",
        "config": {
            "bindings": [
                {
                    "condition": "${exchange.request.uri.scheme == 'http'}",
                    "handler": "ClientHandler",
                    "baseURI": "http://www.example.com:8081"
                },
                {
                    "condition": "${exchange.request.uri.path == '/login'}",
                    "handler": {
                        "type": "Chain",
                        "config": {
                            "comment": "Add one or more filters to handle login.",
                            "filters": [],
                            "handler": "ClientHandler"
                        }
                    },
                    "baseURI": "https://www.example.com:8444"
                },
                {
                    "handler": "ClientHandler",
                    "baseURI": "https://www.example.com:8444"
                }
            ]
        }
    },
    "condition": "${matches(exchange.request.uri.query, 'demo=https')}"
}
